rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload images to the 'post_images' folder
    match /post_images/{imageId} {
      allow read: if true; // Allow anyone to read images
      allow write: if request.auth != null;
    }
  }
}
