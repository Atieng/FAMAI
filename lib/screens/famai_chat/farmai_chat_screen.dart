import 'package:flutter/material.dart';

class FamaiChatScreen extends StatelessWidget {
  const FamaiChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Famai Chat Screen'),
    );
  }
}
